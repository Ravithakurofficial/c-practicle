#include<iostream.h>
#include<conio.h>

int main()
{
    int a = 6, b = 4;
    clrscr();

    cout<< "a == b is" << (a == b) << endl;

    cout<<"a > b is " << (a>b) <<endl;

    cout<<"a => b is " << (a => b) << endl;

    cout<<"a < b is " << (a<b) << endl;

    cout<<"a <= b is " << (a<=b) << endl;

    cout<<"a != b is " << (a != b) << endl;

    cout<<"Ravi kumar thakur"<<endl;
    cout<<"IT Shift 2"
    getch();
    return 0;
}